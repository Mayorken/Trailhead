// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

/// @notice Vendored Chainlink price feed interface (just the 5 functions this project
///         needs) rather than pulling in the full Chainlink contracts package, to keep
///         this money-handling contract's dependency surface lean.
interface AggregatorV3Interface {
    function decimals() external view returns (uint8);

    function description() external view returns (string memory);

    function version() external view returns (uint256);

    function getRoundData(uint80 _roundId)
        external
        view
        returns (
            uint80 roundId,
            int256 answer,
            uint256 startedAt,
            uint256 updatedAt,
            uint80 answeredInRound
        );

    function latestRoundData()
        external
        view
        returns (
            uint80 roundId,
            int256 answer,
            uint256 startedAt,
            uint256 updatedAt,
            uint80 answeredInRound
        );
}
